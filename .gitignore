# macOS
.DS_Store
.fastembed_cache/

# Xcode / build
build/
DerivedData/
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
*.xcuserstate
*.profraw
default.profraw

# xcodegen output (regenerable from project.yml)
01_Project/*.xcodeproj

# Swift Package Manager
.build/
.swiftpm/

# Per-machine signing override
**/Debug.local.xcconfig

# Secrets / local
.env
*.local.json

# Personal MCP server config — local dev environment, not part of the app
.mcp.json

# Signing / notary material — NEVER commit (lives in ~/ProgrammingProjects/99-AUTH/)
*.p8
*.p12
*.cer
*.mobileprovision
*.provisionprofile

# Distributables (DMGs/zips) — published via releases, not tracked
04_Exports/

# Session scratch (machine-local)
WORKING_NOTES.md
.claude/logs/
