# Session: 2026-09-01

## Goal
Audit the running app, fix release-blocking bugs, and prepare the unreleased feature set for v1.1.0.

## Progress
- Verified the public/installed app is notarized v1.0.0 build 2 while the process initially running
  was July's newer Debug build under the same version number.
- Bumped the candidate to v1.1.0 build 3; fixed network double-counting by using active physical
  links; replaced snapping graph scale with sample-clocked decay.
- Added a hostless XCTest target: five tests cover interface filtering, ring-buffer order, peak rise,
  decay, and redraw idempotence; all pass.
- Restored panel dragging. The first SwiftUI frame-driven implementation worked but lagged; the final
  version hands the gesture to AppKit's native drag loop and saves position after mouse-up.
- Made Settings hotkey glyphs larger and spaced; user approved the result on screen.
- Produced and validated a universal Developer ID v1.1.0 archive/export in `/tmp`. It predates the
  final drag and typography fixes and must not be published.
- Reconciled stale project status against GitHub, the live product page, website source, and live DMG:
  v1.0.0 is public; D-024/D-025 and today's stabilization remain unreleased.

## Decisions
- D-026 records physical-link network accounting, sample-driven graph decay, native AppKit dragging,
  and the hostless test architecture.

## Next
- Rebuild the signed v1.1.0 archive from the committed tree, run `/check ship`, then notarize, staple,
  package, install-smoke-test, tag, publish, and update the website metadata.
- Implement the updater feed + SilentUpdateKit path after the release artifact/URL and Ed25519 key are
  final; design multi-profile HUDs afterward.

## Resume
- Working tree should be clean and pushed after this log.
- Exact pickup: `/check ship`, then create a fresh archive (not the old `/tmp` export) and execute the
  proven `scripts/package-dmg.sh 1.1.0 <fresh developer-id export>` chain.
- No half-done source edit. Updater and HUD profiles are intentionally not started.
