# Session — 2026-07-03 · Cross-Mac reconcile, build, glyph decision, cross-pollination audit

## Summary
First session on the **M1 Max** for this project. Bootstrapped git (Syncthing had
stripped `.git`), confirmed the build compiles here, settled the two flagged tile
glyphs, then ran a cross-project cross-pollination audit and contributed a shared
Carbon hotkey engine to `zPackages`.

## What happened

### 1. Git bootstrap (Syncthing-stripped Mac)
- No `.git` on disk (Syncthing excludes it; history lives on GitHub `origin`).
  Ran the `git-bootstrap` skill: `init` → attach `origin` (`Xpycode/QuickStatsPanel`,
  HTTPS via `gh`) → fetch → **`--mixed`** reset → verify.
- **THE CHECK was clean** — on-disk source byte-identical to `origin/main`; zero
  tracked drift. The stale PROJECT_STATE warning ("D-018 committed locally, not
  pushed") resolved itself: origin already contains all of D-018 **plus** D-020.
- Regenerated `QuickStatsPanel.xcodeproj` via `xcodegen` (gitignored artifact).

### 2. Build on this Mac
- `xcodebuild` succeeded, **0 Swift warnings** (only the benign AppIntents-metadata
  note). Signing needed an **ad-hoc override** (`CODE_SIGN_IDENTITY="-"`) because
  `Config/Debug.local.xcconfig` — gitignored but **Syncthing-carried** — points at
  the M4 Pro's `Apple Development` cert (`H56HM4MMZS`), absent from this Mac's
  keychain (which has only `Developer ID Application … FDMSRXXN73`). Did **not**
  edit that file (Syncthing would carry the edit back and break the M4 Pro).
  **Open:** add `Config/Debug.local.xcconfig` to Syncthing's `.stignore` for a real
  per-machine fix.
- App launched (agent app, no Dock icon); confirmed default hotkey **⌃⌥⌘Q**
  (Control+Option+Command+Q), no custom binding stored.

### 3. Glyph design pass (D-019 leftover)
- Two tile glyphs were flagged "confirm in design pass": GPU `cpu.fill`, Power
  `bolt.fill`. Presented alternatives (cube/display for GPU; powerplug/gauge for
  Power). **User chose to keep both.** Updated the four `⚠`-flagged comments in
  `Model/StatDescriptor.swift` to record the confirmed decision (symbol values
  unchanged — binary unaffected).

### 4. Cross-pollination audit → D-021
- Studied **StatsWindow** (per-file I/O via `fs_usage`) at user's request — declined
  to port: it needs **root** (sudo FIFO), clashing with QSP's permission-free HUD
  identity. QSP's aggregate `DiskSampler` stays.
- Ran 3 parallel explore agents over **LaunchAway**, **ClipSmart/Aloft**, and
  `zPackages`. Key findings (full detail: `docs/decisions.md` D-021 +
  `docs/CROSS-POLLINATION.md` in each sibling project):
  - `_Published/Aloft` is empty — **Aloft *is* ClipSmart**.
  - All three sibling HUD apps hand-roll the same Carbon-hotkey/panel/dismiss/theme
    skeleton; `zPackages` (`ShortcutKit`, `MacUIKitSettings`, `SilentUpdateKit`,
    `ProcessKit`) is under-adopted. QSP uses none of them.
  - `ShortcutKit` lacked a **global** hotkey engine (its `KeyboardEventMonitor` is
    app-local) — the reason every app hand-rolled Carbon registration.
- **Contributed `GlobalHotKeyMonitor.swift` to `zPackages/ShortcutKit`** — multi-ID,
  permission-free Carbon engine consuming `CustomKeyboardShortcut`. `swift build`
  clean; all 21 ShortcutKit tests pass.

## Files touched
- `01_Project/Sources/QuickStatsPanel/Model/StatDescriptor.swift` — glyph-decision comments (4 lines).
- `zPackages/Sources/ShortcutKit/GlobalHotKeyMonitor.swift` — NEW shared Carbon engine.
- Findings docs: `LaunchAway/docs/CROSS-POLLINATION.md`, `ClipSmart/docs/CROSS-POLLINATION.md`,
  `zPackages/docs/plans/CROSS-POLLINATION-hotkey-panel-family.md`.
- `docs/decisions.md` (D-021), `docs/PROJECT_STATE.md` (Next), this log.

## Next (QuickStatsPanel backlog from D-021)
1. Launch-at-login (`SMAppService`) — highest value ÷ effort.
2. Adopt `ShortcutKit` + new `GlobalHotKeyMonitor`; delete hand-rolled hotkey stack.
3. Update path: `SilentUpdateKit` + ClipSmart's `package-dmg.sh` (also closes D-019 gate).
4. `ResolvedTheme` env-injection before enabling follow-system light/dark.

## Git state
- QuickStatsPanel: on `main`, in sync with origin at session start; glyph-comment +
  doc edits **committed locally, not pushed** (offered).
- zPackages: `GlobalHotKeyMonitor` + plan doc **committed locally, not pushed** (offered).
- ClipSmart: has git; docs-only file added, **not committed** (left for its own session).
- LaunchAway: no git (Syncthing-stripped); doc file on disk only.
