# Session: 2026-07-11

## Goal
Network tile: show download **and** upload in the strip itself, so upload no
longer requires clicking open the detail card. Then publish a notarized build.

## Progress
- **Cross-Mac pre-flight first:** this Mac (M4 Pro) was 3 behind origin with 10
  Syncthing-carried phantom edits. Verified file-by-file they were byte-identical
  to origin, then stashed + `git pull --ff-only` (the risk-guard hook rightly
  blocked a bare `reset --hard`). Nothing lost; in sync before editing.
- **Network ↓/↑ tile** (`1534e9c`): the tile now reads `↓ 2,3 MB/s  ↑ 480 KB/s`.
  - `StatDescriptor` gained optional `secondaryValue`/`widestSecondaryValue`
    (defaulted init params — every other stat untouched).
  - `StatTileView` renders the second section in its **own** reserved
    worst-case-width slot (second hidden-template ZStack), so the up-field never
    slides as the down-value's digit count changes — D-008 jitter discipline kept.
  - `StatsStripView` passes the two fields through. Detail card keeps Down/Up rows.
  - Clean build, 0 Swift warnings; **user-verified on screen ("works")**.
- **Published — first distributable artifact:** Release archive → developer-id
  export (Developer ID Application, hardened runtime, secure timestamp) →
  `notarytool submit --wait` = **Accepted** → `stapler staple` OK → `spctl` =
  `accepted / Notarized Developer ID` → `04_Exports/QuickStatsPanel-0.1.0-notarized.zip`.
  - This Mac had no `quickstatspanel-notary` keychain profile (that lives on the
    M1 Max); used per-invocation `--key/--key-id/--issuer` flags with the synced
    `99-AUTH/AuthKey_*.p8` instead — no credential store touched.
- Confirmed the app icon ("Abstract bars", 2026-06-05) is wired: `AppIcon.icns`
  in the bundle, `CFBundleIconFile = AppIcon` — the published app carries it.

## Decisions
- None ADR-level. The two-slot tile extension follows the existing D-008
  worst-case-width pattern; Power's single-string split (`8·4 W`) was rejected for
  Network because per-direction units differ and the string-length variance would
  make the ↑ section jitter.

## Next
- Version is still **0.1.0** — decide whether the next publish bumps to 1.0.0
  (`project.yml` MARKETING_VERSION) and sets `LSApplicationCategoryType` (archive warns).
- DMG packaging (`package-dmg.sh` from ClipSmart) + `SilentUpdateKit` — D-021 backlog.
- Decide the artifact's destination (GitHub Release?) — zip currently lives only
  in `04_Exports/` on this Mac (gitignored build artifact).
- Rest of D-021 backlog: ShortcutKit adoption, ResolvedTheme, Settings search.
