# Session Index — QuickStatsPanel

| Date | Session | Summary |
|------|---------|---------|
| [2026-07-12](2026-07-12.md) | v1.0.0 released | Version 1.0.0 (build 2) + Utilities category; new `scripts/package-dmg.sh` (ClipSmart-derived, verify-not-resign, both-Mac notary auth); DMG + app both notarized/stapled, Gatekeeper-accepted; README written (doubles as website copy, icon exported); GitHub release v1.0.0 cut with DMG+zip. Open: repo private → public download path for the site; real screenshot. |
| [2026-07-11](2026-07-11.md) | Network ↓/↑ inline + first publish | Network tile now shows download AND upload in the strip (optional secondary value slot, jitter-free per D-008; `1534e9c`, user-verified). Reconciled M4 Pro's Syncthing phantom copies with origin first. Then published: notarize → Accepted → staple → spctl OK → `04_Exports/QuickStatsPanel-0.1.0-notarized.zip`. Open: 1.0.0 bump, DMG, artifact destination. |
| [2026-07-04](2026-07-04.md) | Status check + push pending commit | Ran `/status`; confirmed `f71dda8` (launch-at-login + cursor/hotkey fixes, D-022) was unpushed and pushed it to `origin/main`. Considered migrating `PROJECT_STATE.md` to the lean digest shape but held off — user just wanted the push done. Docs (`decisions.md`, `PROJECT_STATE.md`, session logs) from the prior session are still uncommitted. |
| [2026-06-04](2026-06-04.md) | Setup + v0 scaffold | New project → Directions + folders + swift MCP profile; design locked (D-001–D-007); scaffolded `01_Project`, builds clean, smoke-launched OK |
| [2026-06-04](2026-06-04.md) | Visual verify + Disk sampler | Panel verified on screen; content-driven + fixed-width tiles (D-008, Penumbra TC pattern); added permission-free DiskSampler (statfs + IOKit), capacity + live I/O |
| [2026-06-04](2026-06-04.md) | v1.1 stats: Network + Battery | NetworkSampler (getifaddrs, count-everything filter) + BatterySampler (IOKit Power Sources, hidden on desktop, inverted color); all 5 tiles verified live on screen |
| [2026-06-04](2026-06-04.md) | Data-driven strip refactor | StatKind + StatDescriptor + StatsStore.visibleStats; StatsStripView collapsed to a ForEach (75→12 lines); fixed type-checker strain, no behavior change; unblocks stat selection |
| [2026-06-04](2026-06-04.md) | In-panel settings (all five) | AppSettings singleton (UserDefaults) + PanelAnchor; gear→self-managed Settings NSWindow (D-009, fixed showSettingsWindow: no-op under LSUIElement); SettingsView form: stat reorder/toggle, interval, anchor, height, hotkey recorder (reject Shift-only). Verified on screen |
| [2026-06-05](2026-06-05.md) | Esc-to-dismiss (D-010) | Bare Escape as a 2nd Carbon hotkey scoped to panel-visible (permission-free, no focus theft); new PanelWindowController.onVisibilityChanged drives teardown across all hide paths; HotKeyService made ID-filtered (latent multi-handler bug fixed). Builds clean |
| [2026-06-05](2026-06-05.md) | Phase A stats (D-011/D-012) | Agent-researched + built 3 permission-free tiles: Load Avg (getloadavg), Uptime (kern.boottime not systemUptime), Top Process (libproc, top *user* process by memory — XNU same-user gate, no root helper). knownStats migration defaults new tiles on safely; SettingsView height scales. Zero view changes. Builds clean; all 8 tiles verified live on screen |
| [2026-06-05](2026-06-05.md) | Top-process lists in popovers (D-013) | iStat-Menus style: rewrote TopProcessSampler → top-5 by CPU%/memory/disk-I/O from one rusage_info_v4 pass; folded into CPU/Mem/Disk popovers, retired standalone Top Process tile (auto-migrates). Verified mach-unit CPU% math (XNU/osquery#7459 — ri_*_time is mach ticks not ns). Network per-proc excluded (no permission-free API). Builds clean, runs; on-screen click-through + Activity Monitor cross-check pending |
| [2026-06-05](2026-06-05.md) | App icon + design-handoff renders + git init (parallel) | Built "Abstract (bars)" AppIcon.appiconset from the design handoff via a native Core Graphics generator (oklch→sRGB by hand; tallest bar glows per README prose); wired `ASSETCATALOG_COMPILER_APPICON_NAME`, build-verified (`AppIcon.icns` emplaced). Rendered the JSX prototype to 7 PNGs via a flat-render harness + WebKit/Playwright (no Chrome). `git init` on `main` (baseline `c450d6d`); kept to disjoint files — other session's edits left untouched |
| [2026-06-13](2026-06-13.md) | Flat detail card (D-015) + Settings verified | Replaced the native `.popover` (un-flattenable system chrome — big radius, material, arrow) with a self-drawn card: `StatDetailView` in its own borderless non-activating `NSPanel` (`DetailPanelController`), flat Theme fill + 12pt radius + no arrow, anchored below the strip, live-reading the store, sized once per open. Tile dropped popover state → `onTap` closure; card hides with the strip via existing `onVisibilityChanged`. Verified on screen; committed `fa5fc05`. Also: confirmed Settings stat on/off toggles already existed & work (closes the click-through item); scoped Themes (named presets) to backlog |
| [2026-06-05](2026-06-05.md) | Top-process lists → `/usr/bin/top` (D-014) | Fixed the D-013 lists via 4 symptoms: sparse (dropped noise thresholds, rows 5→10), resize-jitter (fixed-height reserved slots), wall-of-0% (1-decimal locale %), and missing system procs. Probes proved XNU same-user-gates ALL per-proc CPU/mem APIs (rusage *and* proc_pidinfo) → in-process can never see WindowServer/kernel_task. Rewrote TopProcessSampler to parse `top` (Apple-entitled binary, no user perm), gated to panel-visible, grouping kept via proc_pidpath, disk-I/O list dropped, own-probe PID filtered. Verified on screen vs Activity Monitor. Docs: D-014 added + D-013 superseded; PROJECT_STATE compressed <100 lines |
| [2026-06-13](2026-06-13.md) | First-run hint + code signing | Built queue item #1: one-time first-run hint card (HintPanelController sibling of DetailPanelController + FirstRunHintView; AppSettings.hasSeenHint, mark-seen-on-first-display, dismisses with the strip). Fixed a t=0 anchor/centering bug (deferred 0.2s so the content-driven strip settles after async samples land). Stood up per-machine signing via Config/Debug.local.xcconfig (gitignored, Manual) included by tracked Debug.xcconfig + project.yml configFiles; team is the cert OU (FDMSRXXN73), not the name paren. Clean build, signed, CGWindowList probe verified hint centered + 6pt gap. Open: user visual + hint copy refinement |
| [2026-06-13](2026-06-13.md) | Close out queue item #1 (hint copy + persistence) | Finalized hint copy to a "warm welcome" voice ("You're all set 👋 / Press ⌃⌥⌘Q anytime to pop up your stats.") — literals-only edit, clean signed build (SourceKit Theme-not-found diagnostics are indexer false positives, xcodebuild compiled fine). Settled the "slider/recorder persistence unverified" flag by inspection: `defaults read` shows only `hasSeenHint`/`knownStats` (write-on-change semantics), and `hasSeenHint` surviving to disk proves the identical `didSet→defaults.set` path the sliders/recorder use. Reset hint flag + relaunched for user eyeball. Open: user on-screen visual; commit pending |
| [2026-06-13](2026-06-13.md) | Themes: spec → plan → Wave 1 | Research-driven kickoff of the Themes feature (3 parallel agents + Apple MCPs + web). Wrote `specs/themes.md` (11 ACs; locked: follow-system light+dark, presets+custom, Default/Mono/Vitals/Neon, unified `usageColor(zones:reversed:)`+hysteresis; architecture = theme on the existing `AppSettings.shared` singleton, NOT `@Environment`, since the 3 NSHostingView roots don't share an environment) and `IMPLEMENTATION_PLAN.md` (12 tasks/5 waves around the `Theme` static→struct chokepoint + transitional facade). Executed Wave 1 via 3 parallel developer agents → `Model/Theme/{ColorCodable,ThemePreset,StatusBand}.swift` (sRGB Color↔Codable bridge; preset/density/custom-payload types; `resolveBand` with ±5% hysteresis + `reversed` flag that retires the battery `100-percent` hack). Zero behavior change, clean signed build, commit `3fe3bb4`. Caught a silent agent file-write miss via `git status` line count (build passed regardless — nothing references the types yet) |
| [2026-06-13](2026-06-13.md) | Themes Waves 4+5 → **feature complete** | Finished Themes. W4 Settings UI (in-orchestrator, sequential): "Appearance" `Picker` bound to `themePreset` (live-switches via `didSet→rebuildTheme`, zero glue) + `Customize…` disclosure (ColorPickers/Sliders/density) wired through a generic `customBinding<T>(WritableKeyPath<ThemeData,T>)` that reads `customTheme ?? .default` and reassigns the whole struct (the only thing `didSet` sees → persist + live rebuild); `colorBinding` bridges `Color↔CodableColor`; reset clears theme. W5 verify: `NSObservationTrackingEnabled=YES` in the hand-authored Info.plist (web-verified Apple macOS-15 opt-in; future-proofing, SwiftUI path already live), `code-reviewer` pass (no bugs; rejected a stale "undocumented key" flag), and on-screen ACs — drove strip+Settings up via synthetic `CGEvent` ⌃⌥⌘Q/⌘, (no a11y perm needed), **user confirmed "works and works"**. All 11 ACs + 12 tasks done. Commits `8ad4324`/`00d9880`/`7e2ee79`/`46b87d6` |
| [2026-06-13](2026-06-13.md) | ⌘, opens Settings | Added the macOS-standard ⌘, Settings shortcut. No menu bar (LSUIElement) + non-activating panel never key → SwiftUI `.keyboardShortcut` can't receive it, so reused the scoped Carbon hotkey pattern Esc uses (D-010): new `.commaSettings` binding + `settingsHotKey` (id 3) registered/unregistered in the existing `onVisibilityChanged` closure beside Esc; gear tooltip → "Settings (⌘,)". Trade-off (same as Esc): ⌘, captured globally while panel visible. Clean signed build, user-confirmed on screen. Commits `8a3fc3a` (hint copy) + `8a920b4` (⌘,) |
| [2026-06-13](2026-06-13.md) | Push backlog to origin + Mac handoff | `git push` synced the 14 local commits (Themes Waves 1–5 + ⌘, shortcut + docs) to `origin/main` (`0f13b04..2268787`, fast-forward). No code changes; multi-Mac repo back in sync. Added a **handoff block** (Session 7 log): other Mac must `git pull` first, recreate the gitignored `Debug.local.xcconfig` signing file locally, and `xcodegen generate` (the `.xcodeproj` isn't committed) before building |
| [2026-06-15](2026-06-15.md) | GPU + Fan stats: pick → spec → plan → Wave 1 (D-017) | Kicked off the GPU/temps/fans roadmap item, split into D-017 (GPU+Fan tiles, this session) + D-018 (Temps, later — tile = public `ProcessInfo.thermalState`, per-sensor °C demoted to best-effort detail). Research (web + live `exelban/stats` source) confirmed all paths permission-free: GPU = `IOAccelerator PerformanceStatistics`, Fans = `AppleSMC` keys, Temps = IOHID (riskiest → deferred). Wrote `specs/gpu-fan-stats.md` (8 ACs) + `IMPLEMENTATION_PLAN.md` (4 waves; drop-in SMC/GPU code distilled from live source embedded). Executed **Wave 1** via 2 parallel developer agents → `Sampling/SMC.swift` (read-only AppleSMC; correct `flt` native-LE decode vs big-endian ints — the thing SMCKit gets wrong) + `Sampling/GPUSampler.swift`. Each verified standalone via `swiftc` (avoiding a parallel-build race), then one integrated build: SMC `FNum=2`/range 2317-7826 rpm (flt proven), GPU `AGXAcceleratorG16X`/15% — `** BUILD SUCCEEDED **`, 0 warnings. Commits `2af2818`+`04a808a`. Next: Wave 2 FanSampler |
| [2026-06-15](2026-06-15.md) | GPU + Fan stats: Waves 2–4 → **D-017 SHIPPED** | Finished D-017 inline. **W2** `Sampling/FanSampler.swift` + `FanSample` (fastest fan headline; `loadPercent` normalizes current rpm in `[min,max]`, guards `max>min`, clamps 0–100); caught a `@Sendable` self-capture warning from over-clever queue confinement (`DispatchQueue.async` needs `@Sendable`, `setEventHandler` doesn't) → reverted to the idiomatic synchronous SMC open/close, race made safe by `SMC`'s `conn != 0` guard. **W3** wired `.gpu`/`.fan` into `StatKind`+`StatsStore`+`descriptor(for:)` as one compile unit (exhaustive switches forced full coverage); Battery-style nil-gating. **W4** on-screen (M4 Pro): GPU % tracked Activity Monitor; fans 0 rpm cool → **2483 rpm under 12-core stress** (proves live `flt` decode, AC-3/6); migration verified (new tiles ON+appended, deliberately-off Network preserved, AC-7); AC-2/4 hide by mechanism (no VM/fanless HW — honest limit); `powermetrics --samplers smc` dead on Apple Silicon. 0 warnings. Commits `1dc398a`+`31bf97f`+docs. **Power-in-watts → new D-019** (IOReport, since `powermetrics` needs root). Plan archived |
| [2026-06-15](2026-06-15.md) | Move + "Keep on Screen" pin (D-016) | Made the strip repositionable and stickable. **Drag-to-move** (`isMovableByWindowBackground`; only gaps drag, taps don't) persisted as `AppSettings.customPosition` that wins over `anchor`; user-drag told from `setFrame` echoes via `lastAppliedOrigin`. **Right-click `NSMenu`** (rightMouseDown override — SwiftUI `.contextMenu` unreliable on a non-key panel): Keep on Screen / Reset Position / Settings / Quit. **Pin** = per-summon `isPinned` gating only click-away (Esc/toggle still dismiss); resets on every hide. **Rebindable ⌃⌥⌘P** via a 4th scoped HotKeyService (D-010 pattern) + Settings row. Fixed **Settings opening behind the floating strip** (`openSettings` now hides the strip first). **Pinned thumbtack** in a reserved inline leading slot (not an overlay — frozen-width strip; overlay collided with tile 1). `isPinned` moved onto `@Observable AppSettings` for the live indicator. Clean build, 0 warnings, all five user-verified on screen |
| [2026-06-15](2026-06-15.md) | Power tile via IOReport (D-019) — **SHIPPED** | Built the user-requested permission-free **⚡ CPU·GPU split-watts** tile via `/execute` (4 waves). **W1** (the whole risk, isolated): project's **first bridging header** + `-lIOReport` (no `dlopen`) → `Sampling/IOReport.swift` `IOReportPower` — queue-confined "Energy Model" subscription, ΔJoules÷Δseconds with **runtime unit-label divisor** (mJ/uJ/nJ), **prefix/suffix routing** (multi-die safe), delta-per-tick (first tick `nil`→`0·0 W`), `isAvailable` probed once, full CFRelease accounting; validated un-elevated (CPU→~6 W under load, GPU/ANE flat). **W2** `PowerSampler`+`PowerSample`: CPU·GPU integer headline, session-`peakTotal` ratchet → stored `loadPercent`, pure `makeSample` shared with the DEBUG dump. **W3** wired `.power` into `StatKind`+`StatsStore`+`descriptor` (one compile unit, un-gated, hide-gated like Battery/GPU/Fan). **W4** on-screen (user): tile + fixed-width slot + 5-row detail card; migration AC-9 data-layer-verified; RSS flat. Commits `5778f09`+`fa713ff`+`0a520bd`+docs. **Honest limits:** AC-6 hide-on-Intel by mechanism only; exact `powermetrics` cross-check left to user; **notarytool+`-lIOReport` unproven (release gate, not dev-blocking)**. Plan archived. Next: D-018 Temps |
| [2026-06-15](2026-06-15.md) | Push backlog + D-018 Temps: decide → plan → Wave 1 | Pushed the 12-commit backlog (`812251c..afe51da`, ff). Researched the last roadmap stat (2 parallel agents: codebase wiring + macOS APIs/live OSS) → **D-018 Temperatures**: headline = public `ProcessInfo.thermalState` badge (Nominal/Fair/Serious/Critical → **always-visible tile**, first non-hiding optional one), detail = private-un-entitled **IOHID** named sensors (chosen over SMC's per-machine identity-less Apple-Silicon keys). Wrote D-018 + `IMPLEMENTATION_PLAN-temperatures.md` (4 waves/10 ACs). **Executed Wave 1** (de-risked in isolation): standalone `swiftc` IOHID spike links+runs un-elevated, then `Sampling/TemperatureReader.swift` + bridging-header IOHID block + `project.yml` `-framework IOKit` build clean in-app, 0 warnings. **Finding:** this M4 Pro exposes `PMU tdie*`/`gas gauge battery`/`NAND CH*` (not role-named CPU/GPU sensors) + `PMU tdev*` −9200 °C sentinels → honest detail = SoC/SSD/Battery; `role(for:)` auto-upgrades to CPU/GPU where named, clamp drops sentinels. Wave 1 uncommitted. Next: Waves 2–3 (sampler + tile) |
| [2026-06-15](2026-06-15.md) | D-018 Temps: Waves 2–4 → **SHIPPED** | Finished the last roadmap stat. **W2** `TemperatureSampler`+`TemperatureSample` (state word headline, 0/40/75/100 `loadPercent`→shared tint, per-role °C `detailRows` with `Pressure:` fallback). **W3** one compile unit: `.temps`→`StatKind`+`StatsStore` (un-gated)+`descriptor` with **no `guard`** (first always-visible optional tile). **Launch crash caught in verify → fixed** (`a73dea8`): IOHID event-system client was a local in `init`, ARC freed it at init's end while cached service handles lived on → use-after-free heap corruption surfacing in an unrelated CF bundle-string lookup; held in a stored `let` now (the 5-tick Wave-1 spike missed it). **On screen (M4 Pro):** tile after Power = `Nominal`, detail SoC 36 / SSD 33 / Battery 31 °C, no jitter (AC-1/3/6). Migration AC-7 verified (`knownStats` gained `temps`, deliberately-off stat preserved). Docs + stale `SMC.swift` comment fixed; plan archived. Commits `2e3d315`+`9d18595`+`c5e9349`+`a73dea8`. **Roadmap complete.** Next: push |
| [2026-06-16](2026-06-16.md) | Nicer Settings window: sidebar layout + ⌘, main menu (D-020) | Rewrote `SettingsView` from one tall scrolling `Form` into a `NavigationSplitView` sidebar (ClipSmart pattern) with 5 panes — Stats / Appearance / Panel / Hotkeys / About. All behavior moved verbatim into per-pane structs (each its own `@Bindable AppSettings.shared`); About gained app icon + version + a `confirmationDialog` on Reset. `SettingsWindowController` made `.resizable` (600×460, min 560×420) since a split view fills rather than hugs. Two on-screen bugs fixed: sidebar selection (needed `id: \.self` so the tag is the element, not its String id) + spurious Stats scrollbar (bare List fills the pane instead of fixed-height List in a Form). Then gave ⌘, a real home: `AppDelegate.installMainMenu()` (App/Edit/Window) so ⌘,/⌘W/⌘Q/edit shortcuts fire when the app is active — complementing the strip-scoped Carbon ⌘,; rejected a global ⌘, (would hijack every app). Menu bar stays hidden during glances (strip is `.nonactivating`). 0 warnings; user verified "better." Not a roadmap stat — pure UX polish. |
| [2026-07-03](2026-07-03.md) | M1 Max reconcile + build + glyph decision + cross-pollination audit (D-021) | First session on the M1 Max: `git-bootstrap` (Syncthing had stripped `.git`) — THE CHECK clean, on-disk == origin, D-018+D-020 already pushed. Build clean here (0 warnings) via ad-hoc signing (Syncthing-carried `Debug.local.xcconfig` names the M4 Pro's cert). Glyph design-pass: user kept GPU `cpu.fill` + Power `bolt.fill` (comments de-flagged). Declined porting StatsWindow's `fs_usage` per-file I/O (needs root → breaks permission-free identity). **Cross-pollination audit** (3 parallel agents) of LaunchAway + ClipSmart/Aloft + `zPackages` → **D-021**: all three sibling HUD apps hand-roll the same Carbon-hotkey/panel/dismiss/theme skeleton; `zPackages` under-adopted. Contributed `GlobalHotKeyMonitor.swift` (multi-ID permission-free Carbon engine) to `ShortcutKit` — the global engine it lacked; `swift build` + 21 tests green. Per-app findings logged to each project's `docs/CROSS-POLLINATION.md`. |
| [2026-07-03](2026-07-03.md) | Launch-at-login + cursor/hotkey fixes + **notarization PROVEN** (D-021) | Shipped **launch-at-login** (`SMAppService.mainApp` — non-sandboxed, main bundle is the login item, OS registry is source of truth; "Startup" section in the Panel settings pane). Fixed two user-found on-screen bugs: **"At cursor" stuck** (a saved D-016 drag `customPosition` always beat the anchor; a nudge of the movable strip background pinned it — cursor mode now always re-homes to the pointer + never persists drags) and **⌘,/Esc dead** (each `HotKeyService` installed its own Carbon handler, but same-target handlers run newest-first and stop at the first returning `noErr` → only the last-registered fired; consolidated to one shared handler + id→callback registry; also un-broke toggle-to-dismiss-while-visible). Closed the Syncthing signing-cert leak (`**/*.local.xcconfig` → `.stignore`, fleet-wide). **⭐ Proved the D-019 notarization gate:** Release archive → developer-id export → `notarytool --wait` **Accepted** → staple → `spctl` = `Notarized Developer ID` → notarized build runs; private frameworks (IOReport/IOHID) clear notarization with no `disable-library-validation` (dyld-shared-cache symbols) — `dlopen` fallback unneeded. Committed `f71dda8` (ahead 1). Next: DMG script, v1.0.0, `/check ship`. |
