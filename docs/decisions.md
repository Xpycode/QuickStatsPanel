# Decisions Log — QuickStatsPanel

This file tracks the WHY behind technical and design decisions.

---

## Template

### [Date] - [Decision Title]
**Context:** [What situation prompted this decision?]
**Options Considered:**
1. [Option A] - [pros/cons]
2. [Option B] - [pros/cons]

**Decision:** [What we chose]
**Rationale:** [Why we chose it]
**Consequences:** [What this means going forward]

---

## Decisions

### 2026-06-04 - D-001: HUD `NSPanel`, not the App Shell Standard
**Context:** The global App Shell Standard mandates HSplitView + sidebar + FCPToolbarButtonStyle for macOS apps. QuickStatsPanel is a transient, hotkey-summoned overlay — not a window with chrome.
**Options Considered:**
1. App Shell Standard (HSplitView window) — wrong interaction model; a sidebar/toolbar makes no sense for a glance HUD.
2. `NSPanel` (`.nonactivating`, floating level), summoned over whatever app is focused — matches the MousePlus ring overlay.
**Decision:** `NSPanel`, adapting MousePlus `RingWindowController`.
**Rationale:** A quick-glance overlay must appear *without stealing focus* from the current app and vanish on dismiss. That is precisely what a non-activating panel provides; an HSplitView window cannot.
**Consequences:** `/shell-check` must NOT be run against this app expecting HSplitView. The `Theme` struct still governs panel *content* styling. Documented in CLAUDE.md.

### 2026-06-04 - D-002: Global hotkey via Carbon `RegisterEventHotKey`
**Context:** The app is summoned only by a global hotkey (no menu bar, no Dock). The hotkey must work while any app is focused.
**Options Considered:**
1. `NSEvent.addGlobalMonitorForEvents` (MousePlus's approach) — requires Accessibility/Input Monitoring permission; can only *observe*, and adds first-launch friction.
2. Carbon `RegisterEventHotKey` — system-level hotkey registration, **no special permission**, fires reliably regardless of focused app. API is old (Carbon) but fully supported on macOS 15.
**Decision:** Carbon `RegisterEventHotKey` for a toggle (press → show, press → hide).
**Rationale:** The user explicitly wants minimal friction and to "get away from" system clutter. Requiring an Accessibility grant on first launch contradicts that. MousePlus needed press+release (hold-to-show ring) so it had to use NSEvent; QuickStatsPanel only needs a toggle, so the permission-free path is available.
**Consequences:** Need a tiny Carbon bridge (`HotKeyService`). Hotkey rebinding UI must re-register. If the hotkey conflicts with another app, the panel silently won't appear — mitigate with a sensible default + in-panel rebind.
**Addendum (2026-06-04):** User drives the trigger via **BetterMouse** (mouse button → synthesized key combo, e.g. ⌃⌥⌘Q or ⌃⌥⌘S). `RegisterEventHotKey` catches system-posted *synthetic* key events, so the BetterMouse → hotkey chain works with no Accessibility permission. **Default hotkey: ⌃⌥⌘Q, user-rebindable** in the in-panel settings. ⚠ Verify during implementation that BetterMouse's synthetic keystroke is seen by the hotkey registration (post via CGEvent → should be).

### 2026-06-04 - D-003: `LSUIElement = YES` — no Dock icon, no menu-bar item
**Context:** User wants to avoid the menu bar entirely ("sorting through too many items is cumbersome; menu-bar managers are hampered now") and has no need for a Dock icon.
**Options Considered:**
1. Menu-bar status item (typical utility-app pattern) — rejected by user.
2. Dock + menu bar — heavier, more clutter.
3. Pure agent app (`LSUIElement=YES`), no status item; settings & quit reached from inside the panel.
**Decision:** Option 3.
**Rationale:** Directly serves the user's stated preference. The panel itself becomes the only UI surface.
**Consequences:** The global hotkey is the *only* entry point — a single point of failure (see D-002 mitigations). Settings (incl. hotkey rebind) and Quit MUST be reachable from within the panel (a gear button). First-launch needs a one-time discoverability moment (e.g. show the panel once on first run with a hint).

### 2026-06-04 - D-004: Build with xcodegen (`project.yml`)
**Context:** Need a real `.app` bundle (Info.plist with `LSUIElement`, entitlements, signing). Two local templates exist: StatsWindow (SPM) and MousePlus (xcodegen).
**Options Considered:**
1. Swift Package (StatsWindow's approach) — app-bundle config (LSUIElement, icon, signing) is awkward via SPM.
2. xcodegen `project.yml` — declarative, regenerable, clean app-target config; documented in cookbook `47`.
**Decision:** xcodegen.
**Rationale:** App-bundle metadata is first-class in xcodegen; matches the cookbook and MousePlus.
**Consequences:** `project.yml` lives in `01_Project/`. Re-run `xcodegen generate` after adding files. `.xcodeproj` can be gitignored (regenerable) per cookbook guidance.

### 2026-06-04 - D-005: Port StatsWindow samplers rather than rewrite
**Context:** StatsWindow (same machine) already has working `CPUSampler`, `MemorySampler`, `FSUsageSampler`.
**Decision:** Copy these samplers into QuickStatsPanel, adapt naming, keep the `DispatchSourceTimer` + `Sendable`-sample + callback shape.
**Rationale:** They're clean, concurrency-correct (`SWIFT_STRICT_CONCURRENCY: complete`), and battle-tested. Rewriting Mach host-statistics code is error-prone and pointless.
**Consequences:** Network/Battery/GPU samplers (not in StatsWindow) follow the same shape when added. Attribute the source in a header comment.

### 2026-06-04 - D-006: Thin strip + detail-on-demand, not a tall dashboard
**Context:** Initial proposal was a ~620×120 panel. User: 120 height is too much — wants ~22–44pt, with detail revealed on hover/click of a stat element.
**Options Considered:**
1. Tall dashboard (~120pt) — shows everything at once; intrudes on screen; covers content when anchored near cursor.
2. **Thin strip (~36pt, configurable)** — single row of compact stat tiles (SF Symbol + primary value, e.g. `􀫥 34%`); detail breakdown revealed per-tile on demand.
**Decision:** Option 2. Panel is a thin horizontal strip; each stat tile expands to a detail view (breakdown + sparkline) on interaction.
**Rationale:** User wants a *quick glance*, not a monitoring dashboard. A thin readout intrudes minimally and pairs well with "near cursor" anchoring. Depth stays available without permanent clutter — matches the iStat Menus menu-bar-dropdown feel.
**Detail trigger:** **Click-to-expand for v1** (reliable in a non-activating panel), **hover as a later enhancement** (needs `acceptsMouseMovedEvents` + tracking; user said "click would also be fine if easier"). Implemented via `NSPopover` or inline expansion below the tile.
**Consequences:** Layout is a horizontal `HStack` of fixed-height tiles. Width ~620 default but content-driven/variable. Each sampler must expose both a compact value and a richer breakdown. Strip height configurable (~22 single-line ↔ ~44 icon+label+sparkline).

### 2026-06-04 - D-007: Panel position configurable; default near cursor
**Context:** Where should the strip appear when summoned?
**Decision:** Default **near the cursor** (clamped fully on-screen, like MousePlus); user can set a **fixed anchor** instead (e.g. top-center, a corner, or a remembered point).
**Rationale:** Near-cursor means the readout shows up where the user is already looking — least eye travel. But some users prefer a stable, predictable location; making it a setting serves both.
**Consequences:** `PanelWindowController` takes an `anchor` mode: `.cursor` (default) vs `.fixed(position)`. Settings exposes the choice; the cursor-clamp logic from MousePlus `RingWindowController` covers the `.cursor` case.

### 2026-06-04 - D-008: Strip width is content-driven, not fixed
**Context:** The first cut used a fixed ~620pt-wide panel. With only CPU + Memory tiles that left a large band of empty strip, and a fixed width would either waste space or clip once more stats were added.
**Options Considered:**
1. Fixed width (~620) — predictable, but dead space at few tiles and risks clipping as stats grow.
2. **Content-driven width** — ask the SwiftUI tree for its ideal size (`NSHostingView.fittingSize`) at summon; the strip hugs its tiles and grows as stats are added.
**Decision:** Option 2. `PanelWindowController.show()` measures `hosting.fittingSize` once per summon and sizes the panel to it.
**Rationale:** A glance HUD should be as small as its content allows — least screen intrusion, and it scales automatically as the stat set changes (ties into D-006's "grows with more stats").
**Implementation notes:**
- Tile content uses `.fixedSize(horizontal:)` so values never wrap as the width is computed.
- **Per-summon jitter fix:** tiles are fixed-width with monospaced digits (Penumbra `TimecodeView` pattern, cookbook `67`) so a changing readout (e.g. `9%` → `100%`) doesn't reflow the strip. Width is measured **once per summon**, not per tick — the strip never resizes while visible.
**Consequences:** Width is variable (~210pt with CPU + Mem, larger as tiles are added); anchor/clamp math already works off the measured size. Because size is fixed at summon, settings that change content (stat set, height) apply on the *next* summon, not live (see D-009 consequences).

### 2026-06-04 - D-009: In-panel gear opens the standard Settings *window* (not a popover)
**Context:** "Settings live inside the panel" (D-003), but the panel is a non-activating `NSPanel` and the app is `LSUIElement`. The hotkey-rebind setting needs to *capture* keystrokes, which requires a window that can become key — exactly what the panel refuses to do.
**Options Considered:**
1. Gear → SwiftUI **popover** inside the panel — most on-brand, but recording a hotkey needs a focus workaround since the panel never becomes key.
2. **Gear → the standard `Settings` window** — more room, standard form, and it can become key so the hotkey recorder "just works"; costs a focus switch and a separate window.
**Decision:** Option 2. A trailing gear tile opens a settings window. **Mechanism:** a *self-managed* `NSWindow` hosting `SettingsView` (`SettingsWindowController`), **not** the SwiftUI `Settings` scene — `showSettingsWindow:` silently no-ops under `LSUIElement` (focus shifts, no window appears, as observed in testing). The controller does `NSApp.activate(ignoringOtherApps:)` + `makeKeyAndOrderFront`; the SwiftUI `Settings` scene is now an empty placeholder.
**Rationale:** The hotkey recorder relies on a local `keyDown` monitor, which only fires while a window is key. A self-managed window becomes key reliably; the `Settings` scene's action does not in an accessory app, and a popover in the non-activating panel never becomes key at all.
**Architecture:** `AppSettings.shared` (`@Observable`, `UserDefaults`-backed) is the single source of truth shared by `AppDelegate`'s imperative objects and the `Settings` scene. Passive settings (anchor, height, stat order/enabled) are read lazily at summon / in `body`; active settings (interval, hotkey) push via `onIntervalChanged` / `onHotKeyChanged` `didSet` hooks (`store.restart()` / hotkey re-register). Hotkey validation: reject Shift-only — require ≥1 of ⌘/⌥/⌃.
**Consequences:** Supersedes the "fixed anchor = arbitrary point" hint in D-007 — anchor is now an enum (`cursor`/`screenCenter`/`topCenter`/`bottomCenter`). Anchor/height changes apply on next summon (panel size is measured once per summon via `fittingSize`).

### 2026-06-05 - D-010: Esc-to-dismiss via a scoped Carbon hotkey
**Context:** Esc should dismiss the panel. But the panel is a `.nonactivatingPanel` summoned with `orderFrontRegardless()` — it deliberately never becomes key (D-001), so `keyDown` / SwiftUI `.onKeyPress` never reach it; Esc goes to whatever app actually has focus.
**Options Considered:**
1. Global `NSEvent` keyboard monitor — catches Esc regardless of focus, but a *keyboard* global monitor requires Input-Monitoring permission, breaking the permission-free principle (D-002, D-003).
2. Make the panel key on show + local monitor — no system-wide capture, but steals keyboard focus from the user's app, contradicting the non-activating glance design.
3. **Carbon `RegisterEventHotKey` for bare Escape, registered only while the panel is visible** — permission-free, fires regardless of focus, no focus theft.
**Decision:** Option 3. A second `HotKeyService` instance (`id: 2`) registers bare-Escape on panel show and unregisters on hide.
**Rationale:** Same permission-free, focus-independent mechanism already proven for the toggle hotkey (D-002) — the only such option for a panel that never becomes key. The toggle hotkey remains the primary dismiss; Esc and click-away are conveniences.
**Architecture:** Registration is driven by a new `PanelWindowController.onVisibilityChanged` signal (fired in `show`/`hide`), **not** by `AppDelegate.togglePanel()` — so teardown covers *every* hide path (toggle, click-away, Esc itself). Wiring Esc to the toggle call site alone would leave Escape captured after a click-away dismissal. Required a latent fix in `HotKeyService`: the Carbon callback now reads the fired `EventHotKeyID` (`kEventParamDirectObject` → `typeEventHotKeyID`) and filters by per-instance `id`, since Carbon dispatches every press to *all* installed app-level handlers — without filtering, the toggle hotkey would also fire the dismiss action.
**Consequences:** While the panel is visible, bare Escape is captured **system-wide** (every other app loses Esc until the panel hides). Acceptable because the panel is transient and the capture window is exactly its on-screen lifetime. `HotKeyService` is now multi-instance-safe (ID-filtered), so further scoped hotkeys can be added the same way.

### 2026-06-05 - D-011: Add Load Average + Uptime tiles (Phase A quick-wins)
**Context:** Roadmap calls for more stats. Load average and uptime are permission-free single-call reads that fit the existing sampler pattern with zero view changes.
**Decision:** Two new absolute-snapshot samplers (no delta), wired through `StatKind`/`descriptor(for:)`/`StatsStore` like the others.
- **Load Average** — `getloadavg(3)` (libc, no entitlement). Headline = 1-min load (`"1.24"`); color band = `load1 ÷ activeProcessorCount` (saturation, clamped 100); popover = 1/5/15-min + core count.
- **Uptime** — `sysctlbyname("kern.boottime")`, **not** `ProcessInfo.systemUptime`. Headline = compact two-unit `"3d 4h"`; `loadPercent: 0` (no "load" → always-calm tint); popover = uptime + approx boot date.
**Rationale:** `systemUptime` only counts *awake* time, so on any Mac that sleeps it reads low; `kern.boottime` matches `uptime(1)` / Activity Monitor. Both are snapshots → correct on the very first summon (preserves the summon-glance-dismiss feel — no delta-seeding blank frame).
**Consequences:** Files `Sampling/LoadAverageSampler.swift`, `Sampling/UptimeSampler.swift`. Load can exceed the reserved `"88.88"` width on a >99 load (implausible on a desktop) — acceptable.

### 2026-06-05 - D-012: Top-Process tile is "top *user* process by memory" (no privileged helper)
**Context:** A "what's eating my Mac" tile. macOS gates `proc_info` with a same-user check (verified in XNU `proc_security_policy`): enumerating PIDs and reading a process *name/path* works for any process, but reading *memory/CPU* works only for processes owned by the current user — foreign ones (kernel_task, WindowServer, root daemons) return EPERM.
**Options Considered:**
1. Privileged root helper (how Activity Monitor's `sysmond` does it) — true system-wide top, but installs a setuid helper and breaks the zero-permission stance (D-002/D-003).
2. **Unprivileged libproc, report the heaviest *readable* process** — permission-free; honestly "top user process," missing system daemons. Same model as `top`/htop without `sudo`.
**Decision:** Option 2. **Memory first** (`ri_phys_footprint`, matches Activity Monitor's Memory column), via `proc_listallpids` → `proc_pid_rusage(RUSAGE_INFO_V4)` (skip EPERM) → name via `proc_pidpath().lastPathComponent`. Tile shows the memory value as headline; process name lives in the popover (keeps the fixed-width numeric tile design, D-008, intact). Hidden until the first readable process is found.
**Rationale:** Memory is a snapshot (correct on first summon, no two-tick delta), cheaper than CPU%, and identical permission profile. Top-by-CPU (delta-based, reads blank on the first frame unless sampled continuously) is the documented fast-follow.
**Consequences:** File `Sampling/TopProcessSampler.swift`. Enumerates ~600 PIDs/tick on a background queue (~1–3 ms) even while the panel is hidden — consistent with the other always-on samplers; could be gated to panel-visibility later if power matters. `import Darwin` for libproc. The `proc_pid_rusage` buffer needs a `withMemoryRebound(to: rusage_info_t?.self)` because the C param is an opaque `void *`.
**Also (this change):** `AppSettings` gained a `knownStats` UserDefaults record — new `StatKind`s default ON for existing users without re-enabling stats they deliberately disabled (a disabled stat and a brand-new stat are otherwise indistinguishable, since only enabled stats are persisted). `SettingsView` stat-list height now scales with stat count instead of a hardcoded 170.

### 2026-06-05 - D-013: Top-process lists live in the CPU/Mem/Disk popovers (iStat-Menus style); retire the standalone tile
> **⚠️ Partially superseded by D-014 (2026-06-05):** the popover-folding, app-grouping, and standalone-tile retirement all stand. The **data source** does not — the in-process `rusage` engine (and with it the mach-unit CPU math and the per-process disk-I/O list) was replaced by parsing `/usr/bin/top`, because `rusage` structurally can't see system processes. Read D-014 for the current implementation.

**Context:** Fast-follow to D-012 (top-process CPU mode). Rather than add a second standalone tile or a memory/CPU toggle, fold the rankings into the relevant tile dropdowns — the way iStat Menus does it (CPU dropdown → top by CPU, etc.).
**Options Considered:**
1. Two standalone tiles (top-by-CPU + top-by-memory) — more strip clutter, redundant glyphs.
2. One tile with a memory/CPU toggle — can't see both leaders at once.
3. **Top-N lists inside each tile's existing popover** (CPU/Memory/Disk), retire the `.topProcess` tile — most iStat-like, no extra strip width, each metric's culprits sit under that metric's breakdown.
**Decision:** Option 3. One `TopProcessSampler` enumeration pass per tick reads each PID's `rusage_info_v4` **once** and ranks three ways from that single struct: CPU% (Δ user+system), memory (`ri_phys_footprint` snapshot), disk I/O (Δ `ri_diskio_bytes{read,written}`). Top 5 of each render as a "Top by …" section in the CPU / Memory / Disk popovers. The standalone `.topProcess` `StatKind` is removed.
**Rationale:** All three metrics come free from the rusage struct already read for the old memory tile — one pass, three rankings, same permission profile (still "top *user* processes", D-012's same-user gate). Folding into popovers keeps the thin strip thin (D-006/D-008).
**⚠️ Units gotcha (verified against XNU + osquery#7459):** `ri_user_time`/`ri_system_time` are in **mach absolute time units, not nanoseconds** — the kernel writes `rm_time_mach` straight through (`osfmk/kern/bsd_kern.c` `fill_task_rusage`). On Apple Silicon a tick ≈ 41.67 ns, so treating them as ns under-reports CPU ~24×. Fix: convert via `mach_timebase_info` and measure wall time with `mach_absolute_time()` (for CPU% the timebase cancels mach÷mach; disk bytes/sec needs the real-seconds conversion).
**Network is deliberately excluded:** per-process network bandwidth has **no permission-free public API** on macOS (`nettop` uses the private `NetworkStatistics.framework`; alternatives need root). The Network tile stays system-wide Down/Up only — a hard platform limit, consistent with D-002/D-003.
**Architecture:** `TopProcessSample` → `TopProcessesSample` (`byCPU`/`byMemory`/`byDiskIO: [ProcStat]` + formatting helpers). Sampler keeps per-PID `prevCPU`/`prevDisk` maps + `prevWall` for the rate deltas. `StatDescriptor` gained an optional `ProcessSection` rendered by `StatTileView` beneath the detail rows (auto-hidden while empty, so two-tick CPU/disk rates don't flash an empty header on first summon). `.topProcess` removal needs **no migration** — `AppSettings.init`'s `compactMap(StatKind.init(rawValue:))` silently drops the stale persisted rawValue.
**Grouping (same change):** processes are grouped **by app** — a browser/editor spawns N helper PIDs and a flat list would just repeat the helper name. Each PID is attributed via `appGroupName(forPath:)`, in priority order: (1) **outermost `.app` bundle** in its `proc_pidpath` (first `.app` from the root; how Activity Monitor groups — helpers nested in deeper `.app`s roll up to the top-level app); (2) **`.xpc`/`.appex` service bundle** with no `.app` ancestor, name *prettified* (reverse-DNS → last component → camelCase-split, e.g. `com.apple.WebKit.WebContent` → "Web Content"); (3) plain executable name, but **climbing past version-like tokens and generic launcher dirs** (`versions`/`bin`/`MacOS`/…) so a version-named binary like `…/claude/versions/2.1.165` groups as "claude", not "2.1.165". Values are summed per app, *then* ranked — so 20 small Chrome helpers correctly roll up above a single mid-size process. Grouping defeats the earlier "resolve names for top-8 only" optimization (a PID's group must be known *before* ranking), so names are resolved for every readable PID via a **per-PID `nameCache`** (`proc_pidpath` once per process; pruned to live PIDs each tick so it can't grow unbounded and a recycled PID can't carry a stale name). `ProcStat` is now identified by app `name` (the `pid` field is gone — a row is an app, not a process).
**Rejected — responsible-app attribution for XPC services:** WebKit content/networking procs are XPC services with no `.app` in their path and parent = `launchd`, so they can't be tied to the host app (Safari vs Chrome) from public, permission-free APIs (`ppid` is useless here; ES `responsible_audit_token` needs an Apple entitlement + FDA). The only working route is the **private** `responsibility_get_pid_responsible_for_pid` (libquarantine — permission-free for same-user but unexported, App-Store-rejecting, OS-fragile). Chose to **prettify the name instead** (public-only), consistent with D-012's "stay clean even if data is partial". Consequence: all browsers' content processes group into one summed "Web Content" row, not split per host app (exelban/Stats has the same limitation — closed both grouping requests as not-planned).
**Consequences:** Files: `Sampling/TopProcessSampler.swift` (rewritten), `Model/StatDescriptor.swift`, `Model/StatsStore.swift`, `Views/StatTileView.swift`, `Views/StatsStripView.swift`. `flame`/`Top Process` no longer appear in Settings. Builds clean, no warnings. CPU%/disk-rate accuracy pending on-screen cross-check vs Activity Monitor.

### 2026-06-05 - D-014: Source top-process lists from `/usr/bin/top`, not in-process `rusage`
**Context:** On an idle Mac the D-013 CPU list was near-useless — it showed only QuickStatsPanel + a couple of tiny user procs, never the actual heavy hitters (WindowServer, kernel_task). Root cause, **proven this session with two throwaway probes**: XNU same-user-gates *all* per-process CPU/memory APIs, not just `rusage`. `proc_pid_rusage` **and** `proc_pidinfo(PROC_PIDTASKINFO)` both returned failure/EPERM for kernel_task (pid 0) and WindowServer (`_windowserver`-owned), while succeeding for our own PID. So **no in-process libproc call can ever see foreign processes' CPU** — D-013's "top *user* apps" was hiding a hard wall, and on a quiet machine the genuinely-busy processes are almost always the system ones we couldn't read.
**Key finding:** `top`/Activity Monitor see everything because they are **Apple-signed binaries carrying the private `com.apple.private.proc_info-list` entitlement** that third-party apps cannot obtain. But the entitlement rides with the *binary*, so **shelling out to `/usr/bin/top` works** — the subprocess runs with top's credentials, prints all processes, and we parse it. Still no permission prompt to the user.
**Options Considered:**
1. Keep in-process `rusage`, relabel "Top user apps" — pure/instant/grouped, but structurally blind to system processes (the actual complaint). Rejected.
2. Privileged helper / request the entitlement — true system-wide, but breaks zero-permission (D-002/D-003) and the entitlement is Apple-private/unobtainable. Rejected.
3. **Parse `/usr/bin/top`** — borrows Apple's pre-entitled binary; system-inclusive; no user permission. Chosen.
**Decision:** Option 3, **all lists via `top`** (user-chosen over a CPU-only hybrid). Per tick: `top -l 2 -s 1 -o cpu -stats pid,cpu,mem,command`, read stdout to EOF (~1s), parse the **2nd** sample (whose %CPU is the instantaneous delta top computed — so we do **no rate math**, retiring D-013's mach-unit conversion). `command` is placed **last** so process names with spaces survive whitespace-splitting. CPU% and memory both rank from that one parse.
**Rationale:** Gets the list the user actually wants (looks like Activity Monitor) with no permission, no privileged helper, no private API linkage — just a subprocess of a guaranteed-present system binary. Sandbox is OFF (verified), so spawning is allowed.
**Lifecycle — gated to panel-visibility:** `top` is costly (it showed ~8% CPU itself), so unlike the always-on in-process samplers this one runs **only while the panel is on screen**. `StatsStore.setPanelVisible(_:)` starts/stops it, driven from `PanelWindowController.onVisibilityChanged` (the same hook that scopes the Esc hotkey, D-010). On hide it also clears `topProcesses` so a stale list doesn't flash on the next summon. Cadence is `max(2s, interval)` to leave a gap between the ~1s top runs. List populates ~1s after each summon (headline tiles stay instant).
**Grouping kept; disk-I/O dropped:** app-grouping survives — `proc_pidpath` is permission-free for *any* PID (verified: resolved WindowServer's real path), so we still roll helper soup into one app row, falling back to top's COMMAND for pathless procs (kernel_task). The per-process **disk-I/O list is removed**: `top` has no per-process disk column and the only source is the entitled API behind Activity Monitor's Disk tab. The Disk popover now shows aggregate Read/Write only.
**Observer effect:** our own spawned `top` reports itself in its output (~8%). Filtered out by the **exact PID we spawned** (`Process.processIdentifier`), not by name — so a user's own terminal `top` still appears.
**Consequences:** `Sampling/TopProcessSampler.swift` rewritten (now `import Darwin` only for `proc_pidpath`; spawns `Process`, no `DispatchSourceTimer`-vs-stream change — still timer-driven, but each tick is a self-contained `top` run). `TopProcessesSample` lost `byDiskIO`/`diskRows`. `StatsStore` gained `panelVisible` + `setPanelVisible`. `StatDescriptor` disk case lost its `processSection`. `AppDelegate` visibility hook now also calls `store.setPanelVisible`. **Requires sandbox OFF** (already true; revisit for Mac App Store — `top` can't be spawned under the sandbox, which would force a fallback to D-013's user-only `rusage` engine). Builds clean. Verified on screen: CPU list shows WindowServer/kernel_task with sane per-core % matching Activity Monitor; grouping correct; probe filtered.

### 2026-06-15 - D-016: Drag-to-move + "Keep on Screen" pin (right-click menu + hotkey)
**Context:** The strip is summon-and-dismiss by design (D-006/D-010). Two user asks: (1) reposition it by hand and have it *stay* there across summons; (2) keep it on screen for a longer read instead of click-away/Esc dismissing it. Plus a right-click menu as the home for both, since the app has no Dock/menu-bar surface (D-003).
**Decision:**
- **Drag to move** — panel set `isMovable + isMovableByWindowBackground`; SwiftUI tiles/gear are controls that consume their own mouse-downs, so only the gaps initiate a drag and a plain tap never moves it. The dragged origin is captured and persisted as `AppSettings.customPosition` (a `CGPoint?`), which **wins over `anchor`** at summon time. Choosing an anchor in Settings or "Reset Position" clears it.
- **Pin ("Keep on Screen")** — a per-summon `isPinned` flag. While pinned, **only the click-away monitor is suppressed**; Esc and the toggle hotkey still hide it (user chose "Esc unpins and dismisses", so Esc is deliberately *not* gated). `isPinned` **resets on every hide path** so a fresh summon is never stuck pinned.
- **Right-click menu** — `NSMenu` popped from a `rightMouseDown` override in the panel's hosting view (SwiftUI `.contextMenu` is unreliable on a non-key, non-activating panel). Items: Keep on Screen (✓), Reset Position (enabled only when dragged), Settings…, Quit.
- **Pin hotkey** — default **⌃⌥⌘P**, rebindable in Settings (reuses `HotKeyRecorderView`). Registered **only while the panel is visible** (4th scoped `HotKeyService`, id 4), mirroring Esc/⌘, (D-010). No live re-register hook needed: opening Settings now hides the strip, so a rebind is always picked up on the next summon.
- **Settings-covers-strip fix** — `openSettings()` now calls `panel.hide()` first. The strip floats at `.floating` level and would sit on top of the normal-level Settings window (worse when pinned). Settings is its own full context.
- **Pinned indicator** — a `pin.fill` thumbtack in a **reserved inline leading slot** (always laid out, `opacity` 0↔1), *not* a floating overlay. The strip's width is measured once per summon (`fittingSize`, D-008) and frozen, so an overlay collided with the first tile and an `if`-conditional element would reflow/clip on a mid-display toggle; a reserved slot fades in place with zero layout change. Tinted `secondaryText` (theme-adaptive; avoids the calm/busy/hot band colors that carry stat-status meaning).
**Why `isPinned` on `AppSettings.shared`:** it's transient (not persisted), but lives on the `@Observable` singleton so the SwiftUI strip can react to it live for the indicator; `AppDelegate` keeps a computed `isPinned` forwarding to it so its call sites are unchanged.
**Drag-vs-programmatic discrimination:** the move observer (`NSWindow.didMoveNotification`, block-based + `MainActor.assumeIsolated` to stay clean under `complete` strict concurrency — same shape as `AppSettings`'s accessibility observer, *not* an `NSWindowDelegate` conformance) ignores moves whose origin exactly equals `lastAppliedOrigin` (our own `setFrame` echoes), reporting only genuine user drags.
**Consequences:** Files: `PanelWindowController` (movable panel, `customOrigin` summon path, move observer + `onPanelMoved`, `reposition`, context-menu hook), `AppDelegate` (pin gating, NSMenu builder + @objc actions, 4th hotkey, Settings-hides-strip), `AppSettings` (`customPosition`, `pinHotKey`, transient `isPinned`), `HotKeyService` (`.defaultPin`), `SettingsView` (pin-hotkey row + reset), `StatsStripView` (reserved pin slot). Builds clean, 0 warnings. All five behaviors verified on screen by the user. **MAS note unaffected** (D-014's `top` caveat still the only sandbox blocker).

### 2026-06-15 - D-017: GPU + Fan tiles (IOAccelerator utilization + read-only AppleSMC)
**Context:** The strip covered CPU/Mem/Disk/Network/Battery/Load/Uptime but nothing about the GPU or cooling — two stats users expect on a stats HUD, and the last "core hardware" gaps before temperatures. Increment 1 of 2 in the roadmap's "GPU / temps / fans" item; temperatures are deferred to D-018.
**Decision:** Add two permission-free tiles via the existing `StatKind` → `StatDescriptor` → `StatsStore.visibleStats` pipeline, both **hidden when the hardware is absent** like Battery on desktops.
- **GPU = IOKit `IOAccelerator` registry read** (same shape as `DiskSampler`'s `IOBlockStorageDriver` walk): `IOServiceMatching("IOAccelerator")` → iterate services → `IORegistryEntryCreateCFProperties` → `["PerformanceStatistics"]["Device Utilization %"]` (Int 0–100), fallback `"GPU Activity(%)"`; clamp 0–100. Device name from `props["model"]` (ASCII `Data`, NUL-stripped) with `IOClass` fallback. `nil` ⇒ `isAvailable=false` ⇒ tile hidden. Ships native arm64 (Rosetta reports no GPU).
- **Fans = new read-only `Sampling/SMC.swift`** opening one `AppleSMC` connection (`IOServiceGetMatchingService` + `IOServiceOpen`, connection type 0), two-step `READ_KEYINFO`→`READ_BYTES` handshake via `IOConnectCallStructMethod(conn, 2, …)`. Keys `FNum`/`F<i>Ac`/`F<i>Mn`/`F<i>Mx`. **Read-only forever** — fan *control* needs privileges and carries thermal risk; reading never does.
- **The `flt` decode is the crux:** Apple-Silicon fan keys report the `flt ` type = IEEE-754 single-precision in **native little-endian**, decoded via `withUnsafeBytes { $0.load(as: Float.self) }`. Integer types (`ui8`/`ui16`/`ui32`/`fpe2`/`sp78`) are big-endian and assembled MSB-first. Older readers that only know `fpe2`/`sp78` (SMCKit, osx-cpu-temp) read garbage rpm on M-series — reference reader was exelban/stats `SMC/smc.swift`.
- **`loadPercent` feeds the existing tint pipeline, no per-tile color code:** GPU = utilization % (higher = hotter, `reversed:false`); Fan = fastest fan's current rpm normalized in its own `[min,max]` (near full tilt = hot), guarded `max>min` (unknown range ⇒ 0) and clamped 0–100 (idle below rated min ⇒ 0). Fastest fan (not average) drives the headline so the one actually ramping surfaces.
- **Not visibility-gated** — GPU + SMC reads are cheap in-process reads, so both run continuously like CPU/Disk (only the costly `top` sampler stays gated). `FanSampler` holds its one SMC connection for its lifetime, closed on `stop()`.
- **Migration is free** — `.gpu`/`.fan` in `allCases` ride the existing `knownStats` logic: a kind not in stored `knownStats` defaults ON and appends to `statOrder`. Verified on screen (existing-user sim: new tiles enabled + appended at end; a deliberately-off Network stayed off).
**Rationale:** Both reads sit at the same trust level as the existing permission-free samplers — no entitlement, no prompt, no root, notarizable. Reuses the data-driven strip wholesale (no view changes). The exhaustive `StatKind` switches forced every display path to handle the new cases, so the wiring couldn't silently miss a name/symbol/tile.
**Open / flagged:** GPU tile glyph is a placeholder `cpu.fill` (⚠ reads near "CPU" — flagged for the design pass; one-line swap). On-screen device name shows `IOClass` ("AGXAcceleratorG16X") because this Mac's accelerator exposes no `model` key — accurate but technical; polish candidate.
**Verified on screen (M4 Pro):** GPU % tracked Activity Monitor under load; fans read 0 rpm cool-idle then climbed to **2483 rpm** under a 12-core stress (proving live `F0Ac` decode, AC-3/AC-6); both detail cards correct; AC-7 migration confirmed at data + visual layer. **AC-2/AC-4 (hide on no-GPU/fanless) verified by mechanism** — same `compactMap(descriptor(for:))` chokepoint proven dropping a tile cleanly during the migration test, plus `guard isAvailable/hasFans` + `.empty` flag inspection; **not** tested on real fanless/headless hardware (none available — honest limit).
**Consequences:** New files `Sampling/SMC.swift`, `Sampling/GPUSampler.swift`, `Sampling/FanSampler.swift`. Touched `Model/StatDescriptor.swift` (`.gpu`/`.fan` enum + `displayName`/`settingsSymbol` + descriptor cases) and `Model/StatsStore.swift` (published `gpu`/`fan` samples + sampler ownership). Builds clean from scratch, 0 Swift warnings. **MAS caveat unchanged** — permission-free & notarizable but not App-Sandbox-safe (consistent with D-014's `top`). Power-in-watts via IOReport split to a future increment (D-019); temps to D-018.

### 2026-06-15 - D-019: Power tile — permission-free SoC watts via IOReport "Energy Model"
**Context:** The strip showed every core stat except power draw — the natural companion to the GPU/Fan tiles (D-017) and the obvious step before temperatures (D-018). The system tools that report SoC watts need root (`sudo powermetrics`, `asitop`) or aren't a glance HUD (Activity Monitor's "Energy Impact" isn't watts). QuickStatsPanel's premise is permission-free/no-prompt, so `powermetrics` is disqualified — which is exactly why this is a *separate* decision from D-017.
**Decision:** Add a permission-free **Power tile** showing live **CPU·GPU split watts** (`⚡ 8·4 W`), via Apple's private but **un-entitled** `IOReport` framework's "Energy Model" channel group — wired through the existing `StatKind` → `StatDescriptor` → `StatsStore.visibleStats` pipeline, **hidden when the channels don't resolve** like Battery/GPU/Fan.
- **Linkage (the novel risk, de-risked first in isolation — Wave 1):** IOReport ships **no public header and no Swift overlay**. So the project's **first bridging header** (`QuickStatsPanel-Bridging-Header.h`) declares the C prototypes as plain `extern`s, and `project.yml` links the symbol directly with `OTHER_LDFLAGS: -lIOReport` (+ `SWIFT_OBJC_BRIDGING_HEADER`). It resolves against `/usr/lib/libIOReport.tbd` in the dyld shared cache — **no `dlopen`, no relocated-framework Library-Validation issue**. The opaque-tag typedef uses a distinct tag name (`struct __IOReportSubscription*`) to avoid a tag/typedef-name collision. Swift precedent: NeoAsitop; also socpowerbud (ObjC), macmon (Rust).
- **Subscribe via `IOReportCopyAllChannels(0,0)` + filter `group == "Energy Model"` at read time** — NOT `IOReportCopyChannelsInGroup` (all three reference tools do this; avoids an `NSString*` bridging call).
- **Watts = (ΔenergyCounter ÷ divisor) ÷ dt**, where the divisor comes from each channel's **runtime unit label** (`mJ`→1e3 / `uJ`→1e6 / `nJ`→1e9, default mJ) read via `IOReportChannelGetUnitLabel` — **never hardcoded** (macmon reads all three; NeoAsitop's hardcoded mJ is a known shortcut). `dt` is measured from `CFAbsoluteTimeGetCurrent()` deltas, not the timer's nominal interval, so a late tick can't inflate watts.
- **Delta-per-tick across ticks** (NetworkSampler shape), NOT macmon's blocking sleep-between-two-samples: the wrapper holds the previous raw sample and deltas the current one against it each tick. First tick has no prior → `read()` returns `nil` → the tile shows `0·0 W` (seeding, AC-7), it is **not** hidden.
- **Channel routing by prefix/suffix, summed** (macmon `src/metrics.rs`): `hasSuffix("CPU Energy")`, `== "GPU Energy"`, `hasPrefix("ANE")`, `hasPrefix("DRAM")` — so multi-die Pro/Max/Ultra parts (`DIE_0_CPU Energy`, `ANE0/ANE1`, …) aren't under-reported (AC-8). Exact-match would miss them.
- **`isAvailable` decided ONCE at init** (did the subscription resolve ≥1 CPU/GPU Energy channel?), independent of having a delta yet — so the tile **hides** on Intel/unsupported (AC-6) while still showing a single `0·0 W` priming tick on supported hardware (AC-7). *(Divergence from GPU/Fan, where availability is per-snapshot.)*
- **Headline = CPU·GPU split, integers** (`headlineFormatted` = `"\(Int(cpu.rounded()))·\(Int(gpu.rounded())) W"`); the **Total** (and ANE/DRAM) live in the detail card. *Consequence:* a deep-idle Mac (sub-watt CPU/GPU) reads `0·0 W` on the strip while the 1-decimal detail card shows the real draw — honest, and it resolves the instant there's real load. *(user choice)*
- **Tint = auto-tracked session peak:** `loadPercent` is a **STORED** field on `PowerSample`, set by the sampler from a `peakTotal` ratchet (`total ÷ sessionPeak × 100`, peak ratchets up, never down, reset in `start()` so `restart()` recalibrates). Total = CPU+GPU+ANE+DRAM (open-Q-1 default). Feeds the existing shared tint pipeline — no per-tile color code. *(Divergence from GPU/Fan, whose `loadPercent` is purely computed.)*
- **Not visibility-gated** — IOReport reads are cheap in-process IOKit calls, so the sampler runs continuously like CPU/GPU/Fan (only the costly `top` sampler stays gated).
- **CF accounting (AC-10):** every `IOReportCreateSamples`/`…Delta` result is `Unmanaged` we own; each tick creates one sample (becomes the next baseline) + one delta (released same tick), releasing the previous baseline as the new one is stored — net zero leaked CF objects/tick. The subscription + subscribed-channels dict are released once in `deinit`. All IOReport calls are confined to the sampler's serial queue (the value-type `PowerSample` is the only thing that escapes) — same confinement FanSampler uses for its SMC handle.
**Rationale:** Sits at the same trust level as the existing permission-free IOKit reads — no root, no entitlement, no prompt, notarizable. Reuses the data-driven strip wholesale; the exhaustive `StatKind` switches forced every display path to handle `.power`.
**Fragility (why hide-on-absent is mandatory, not optional):** IOReport is private/undocumented; a new chip generation can rename channels → the tile reads 0 and **hides** (AC-6), it does **not** crash. `guard isAvailable` / nil-descriptor is the safety net.
**Verified (M4 Pro, un-elevated uid 501):** builds 0 warnings, `-lIOReport` in the link line. Live dump: `isAvailable=true`; tick 0 = `0·0 W` (AC-7); idle ≈ CPU 0.8 / GPU 0.2 / ANE 0 / DRAM 0.2 / Total 1.2 W; under a 12-core load **CPU rose to ~6–7 W while GPU/ANE stayed flat and DRAM ticked up** (correct routing + math, AC-1/AC-2/AC-8); `peakTotal` ratchets up monotonically and `loadPercent` normalizes against it (AC-5, dump-proven; tint reuses the shipped pipeline). On screen (user): tile present in position, fixed-width slot (AC-3), detail card shows all 5 rows (AC-4). Migration (AC-9) verified at the data layer — relaunch added `power` to persisted `knownStats`, defaulted ON; `statOrder` append is in-memory (write-on-change), tile renders.
**Honest limits / open:** (1) **AC-6** (hide on Intel/unsupported) verified by mechanism + forced-stub reasoning only — no Intel/VM available (same limit as D-017). (2) The exact `sudo powermetrics --samplers cpu_power,gpu_power` ballpark cross-check is **left to the user** (needs sudo); magnitudes are already plausible and load-responsive. (3) **notarytool (flagged, unproven):** no source confirms a signed+hardened binary linking `-lIOReport` passes `notarytool`. **Not dev-blocking** — notarization isn't wired up (Release still uses Xcode-automatic signing), so this is a **release gate**, not verified this cycle. Fallback if it ever fails: `dlopen`/`dlsym` IOReport at runtime to avoid the link-time symbol (kept in back pocket, not built pre-emptively). (4) Glyph `bolt.fill` and `widestValue "88·88 W"` (Ultra GPU could exceed 99 W → clips, acceptable like D-011) are confirm-on-screen defaults. (5) Long (~10 min) leak soak not run; CF accounting balanced by inspection + short RSS sample flat.
**Consequences:** New files `QuickStatsPanel-Bridging-Header.h` (first in the project), `Sampling/IOReport.swift` (`IOReportPower`), `Sampling/PowerSampler.swift` (`PowerSampler` + `PowerSample`). Touched `Model/StatDescriptor.swift` (`.power` enum + `displayName`/`settingsSymbol` + descriptor case), `Model/StatsStore.swift` (published `power` + sampler ownership, un-gated), `01_Project/project.yml` (bridging header + `-lIOReport`). A `#if DEBUG` `debugDump()` spike helper remains in each sampler for the powermetrics cross-check + future verification. **MAS caveat unchanged** — permission-free & notarizable but **not** App-Sandbox-safe (private framework), same class as D-014 (`top`) / D-017. Fine for direct/notarized distribution. **No new distribution constraint.** Builds clean from scratch, 0 Swift warnings.

### 2026-06-15 - D-018: Temperatures tile — public thermalState headline + best-effort IOHID per-sensor °C
**Context:** The last roadmap stat. The strip reports CPU/Mem/Disk/Network/Battery/Load/Uptime/GPU/Fan/Power but nothing thermal — the companion to D-017 (fans) and D-019 (watts). The honest, permission-free temperature signal splits in two: a *guaranteed* OS pressure level, and *best-effort* per-sensor °C that no public API exposes. Treating them as one layer is the trap (you'd either hide a tile that needn't hide, or show fragile numbers as if guaranteed) — so the tile is built from both.
**Decision:** Add a **Temperatures tile** via the existing `StatKind` → `StatDescriptor` → `StatsStore.visibleStats` pipeline, with a two-layer design:
- **Headline = `ProcessInfo.processInfo.thermalState`** → the word `Nominal` / `Fair` / `Serious` / `Critical`, color-escalated through the existing shared `tint(for:percent:)` pipeline. This API is **public, guaranteed, permission-free** (works on Intel, VMs, any future chip), so the descriptor **never returns `nil`** — the tile is the project's **first always-visible *optional* tile**, a deliberate divergence from Battery/GPU/Fan/Power's hide-when-absent. *(A future reader must not "fix" this into hiding.)* `loadPercent` maps the four states to **0 / 40 / 75 / 100** so the calm→busy→hot color band *and* the non-color font-weight severity ramp both work for free. *(user choice: badge over a hottest-°C number — robust + always-on beats a fragile headline number.)*
- **Detail card = best-effort per-sensor °C via IOHID** (`IOHIDEventSystemClient` + `kIOHIDEventTypeTemperature`). Chosen over reusing the existing `SMC` reader because on Apple Silicon the classic Intel SMC temp keys (`TC0P`/`TG0P`) are **gone**, replaced by undocumented per-cluster keys (`Tp01`/`Tg05`…) that **vary per machine even with the same chip** and carry **no sensor identity** — you'd guess which key is the GPU and maintain per-chip tables forever. IOHID instead returns **named** float sensors (`"pACC MTR Temp Sensor"`, `"GPU MTR Temp Sensor"`, `"SOC MTR Temp Sensor"`, `"gas gauge battery"`), which is what `exelban/stats` / `macmon` / `mactop` ship. Sensors are grouped by a defensive name→role map (`role(for:)`) into CPU / GPU / SoC / SSD / Battery rows, averaged per role, and **sanity-clamped** (drop NaN / ≤0 / >130 °C). **Wave-1 empirical finding (M4 Pro, un-elevated):** page `0xff00/5` is the only one that returns sensors, and this chip exposes **`PMU tdie*` SoC dies (~37 °C), `gas gauge battery` (31 °C), `NAND CH*` (33 °C)** — NOT the role-named `pACC`/`GPU MTR` sensors the reference tools list on other chips, plus `PMU tdev*` sentinels (≈ −9200 °C) and `PMU tcal` (calibration) which the map/clamp exclude. So on this Mac the detail shows **SoC / SSD / Battery** (CPU vs GPU isn't separable from numbered dies); `role(for:)` *upgrades* to CPU/GPU rows automatically on any chip that names them. If IOHID enumerates nothing (Intel / VM / renamed future chip), the detail falls back to a single `Pressure: <state>` row — the tile still shows because the headline is thermalState.
- **Linkage (the novel risk — de-risked first in isolation, Wave 1):** IOHID's event-system-client symbols ship no Swift overlay, so they're declared as plain `extern` C in the **existing** `QuickStatsPanel-Bridging-Header.h` (second consumer after IOReport — the bridging-header pattern is now established, not novel) and resolve via `OTHER_LDFLAGS: … -framework IOKit`. Same no-`dlopen`, no-Library-Validation-issue path as `-lIOReport`. Reference: `fermion-star/apple_sensors` (matching dict + call sequence), `exelban/stats Modules/Sensors/reader.m` (production notarized usage). The temperature page/usage match is `{ "PrimaryUsagePage": 0xff00, "PrimaryUsage": 5 }`; the value field is `IOHIDEventGetFloatValue(event, kIOHIDEventTypeTemperature << 16)`.
- **Poll, don't observe.** thermalState is read each tick inside the sampler alongside the IOHID read — NOT via `thermalStateDidChangeNotification` (which fires on a background queue and needs a priming read). A cheap property read on the timer matches all ten existing samplers; the HUD only ticks ~1 s and only matters while summoned.
- **CF accounting (as shipped):** the bridging-header opaque tags collide *by design* with the SDK's CF-type-registered IOHID types, so Swift imports the client and service handles as **managed CF classes** — ARC does the releasing, no manual `Unmanaged` dance. The `IOHIDEventSystemClient` is held in a **stored `let`** so it lives for the reader's whole lifetime (ARC releases it in the synthesized `deinit`); the service handles are retained in an array; per-tick `IOHIDServiceClientCopyEvent` is `Unmanaged` taken with `takeRetainedValue()` so ARC releases each event — net zero leak/tick. All IOHID calls confined to the sampler's serial queue (only the value-type `TemperatureSample: Sendable` escapes), same confinement Fan/Power use. **Shipped-bug caught in verification:** the first cut made the client a *local* in `init`, so ARC freed it the moment `init` returned while the cached service handles (vended by it) outlived it → reading an event through a freed parent = use-after-free heap corruption that crashed the app on the first sampler tick (surfacing, classically, in an unrelated CoreFoundation bundle-string lookup). Fix `a73dea8`: promote the client to a stored property.
- **Not visibility-gated** — IOHID + thermalState reads are cheap in-process calls; run continuously like CPU/GPU/Fan/Power (only the costly `top` sampler stays gated). **Migration is free** — `.temps` in `allCases` rides the existing `knownStats` logic (defaults ON, appends to `statOrder`).
**Rationale:** Same trust level as every existing permission-free read — no root, no entitlement, no prompt, notarizable. The two-layer split is what makes it honest: the tile is as reliable as the OS thermal API, while the numbers are clearly best-effort and degrade to a fallback row instead of lying.
**Fragility:** IOHID temperature sensors are private/undocumented — sensor *names* drift per SoC generation, so the prefix→cluster mapping is maintenance the headline never needs. A chip that renames everything loses the detail °C, **not** the tile.
**Open / flagged:** (1) Tile glyph — static `thermometer` vs state-aware `thermometer.low/medium/high` (design pass). (2) The 0/40/75/100 state→loadPercent numbers are tunable on screen. (3) Which IOHID prefixes map to CPU/GPU/SoC is finalized from the Wave-1 spike's actual sensor dump on the M4 Pro. (4) Headline could optionally append the hottest °C (`Nominal 72°C`) — deferred; v1 is the pure badge per the user's choice. (5) **notarytool + IOHID symbols unproven** — same release-gate flag as D-019's `-lIOReport`, not dev-blocking; fallback is `dlopen`.
**Distribution caveat unchanged** — permission-free & notarizable but **not** App-Sandbox-safe (private framework), same class as D-014/D-017/D-019. Fine for direct/notarized.
**Status:** ✅ **SHIPPED 2026-06-15** (4 waves / 10 ACs; commits `2e3d315` Wave 1 → `9d18595` Wave 2 → `c5e9349` Wave 3 → `a73dea8` crash fix). Verified on screen (M4 Pro): Temperatures tile sits after Power showing `Nominal`; detail card lists **SoC 36 °C / SSD 33 °C / Battery 31 °C** (exactly the Wave-1-predicted roles for this chip), fixed-width with no jitter. Migration (AC-7) verified — a relaunch of a simulated pre-`temps` user wrote `temps` into `knownStats` and (in memory) enabled it while a deliberately-off stat stayed off. **Launch-crash found & fixed in verification** (use-after-free, see CF-accounting note above). Archived plan `docs/sessions/IMPLEMENTATION_PLAN-temperatures-DONE.md`. Release-gate flags unchanged (notarytool + IOHID symbols unproven — not dev-blocking; not App-Sandbox-safe).

---
*Add decisions as they are made. Future-you will thank present-you.*

### 2026-06-16 - D-020: Settings window — sidebar layout + application main menu for ⌘,
**Context:** The Settings window (a self-managed `NSWindow` under `LSUIElement`, opened from the panel gear) was a single tall scrolling `Form` of five stacked sections — it read long and unstructured as the stat roadmap filled out. User asked for "a nicer settings window," pointing at sibling apps. Two house patterns exist: MousePlus's `TabView` (the closest HUD sibling) and ClipSmart's `NavigationSplitView` sidebar (System-Settings scale). Separately, ⌘, (the macOS Settings shortcut) only worked while the strip was visible — dead inside the Settings window and when the strip was down.
**Decision:**
- **Sidebar layout** (user picked it over tabs): `Views/SettingsView.swift` is now a `NavigationSplitView` with a private `SettingsPane` enum driving the sidebar — **Stats / Appearance / Panel / Hotkeys / About**. Each pane is its own struct holding `@Bindable AppSettings.shared`; all prior binding logic (`customBinding`/`colorBinding`/`enabledBinding`/`resetToDefaults`) moved verbatim, so Observation stays live with no glue. New **About** pane: `NSApp.applicationIconImage` + `CFBundleShortVersionString` + Reset (now behind a `confirmationDialog`, no undo) + Quit. `SettingsWindowController` switched to `.resizable` with a 600×460 default + 560×420 min — a split view **fills** its window (vs the old `.frame(width: 380)` form that hugged), so it needs an explicit size or collapses.
  - Two gotchas (both caught on screen): `List(_:selection:)` tags rows with the element's `Identifiable.id` (String), not the `SettingsPane?` binding → add **`id: \.self`** so the tag is the element (selection was dead without it). And a fixed-height `List` nested in a grouped `Form` inside a fixed window overflows → the **Stats pane uses a bare `List` that fills the pane** (one scroll container) instead.
- **⌘, via an application main menu** (`AppDelegate.installMainMenu()`): a minimal App/Edit/Window menu. Key equivalents in `NSApp.mainMenu` fire whenever the app is **active** (a window is key), so ⌘, opens Settings when the app is frontmost, and the Settings window gains ⌘W (close), ⌘Q (quit), cut/copy/paste. The strip-scoped Carbon `.commaSettings` stays for the glance flow (strip up, app not activated).
**Rationale:** Sidebar matches a shipped sibling and scales as panes grow. A **global** ⌘, was rejected — an always-on Carbon hotkey for ⌘, would hijack the Preferences shortcut in *every* app; ⌘, should mean "my settings" only when this app is the relevant context. The main menu is the conventional, non-intrusive way to get that.
**Identity preserved:** the strip is a `.nonactivating` panel, so summoning it never activates the app — the menu bar stays hidden during a glance and appears only when the Settings window is focused (where a menu bar belongs). "No menu-bar item" (D-003) is about an `NSStatusItem`, which this does not add.
**Consequences:** Touched `Views/SettingsView.swift` (full rewrite), `Panel/SettingsWindowController.swift` (resizable + sized), `AppDelegate.swift` (`installMainMenu()` + call in `applicationDidFinishLaunching`). No model/persistence changes. Builds clean from scratch, 0 Swift warnings (the `#selector(NSText.copy(_:) as ...)` cast avoids the `NSObject.copy()` overload-ambiguity warning).
**Open / flagged:** (1) Sidebar glyphs are placeholders (`square.grid.2x2`/`paintbrush`/`macwindow`/`keyboard`/`info.circle`). (2) Whether the menu-bar-when-Settings-focused behavior is wanted, or swap to a hidden SwiftUI ⌘W shortcut instead of a full main menu — deferred to user.
**Status:** ✅ Shipped + user-verified on screen 2026-06-16 ("better"). Pure UX polish — not a roadmap stat.

### 2026-07-03 - D-021: Cross-pollination audit — converge the hotkey-HUD family on shared packages
**Context:** With the stat roadmap complete, ran a cross-project audit (3 parallel explore agents + direct code reads) of the sibling HUD apps **LaunchAway** and **ClipSmart/Aloft** (note: `_Published/Aloft` is an empty marketing folder — Aloft *is* ClipSmart, bundle `com.lucesumbrarum.Aloft`), plus the shared `zPackages` libraries. Finding: all three apps (QuickStatsPanel, LaunchAway, ClipSmart) independently **hand-rolled the same skeleton** — Carbon global hotkey + non-activating `NSPanel` + click-away/Esc dismiss + theme + self-managed Settings window — while `zPackages` already ships `ShortcutKit`, `MacUIKitSettings`, `SilentUpdateKit`, `ProcessKit` that go under-adopted. QuickStatsPanel uses **none** of them; its D-020 Settings sidebar was a hand-*copy* of ClipSmart's (the drift the packages exist to prevent).
**Decision (this pass):** Contributed the one primitive the shared package was missing — a **multi-ID, permission-free Carbon global-hotkey engine** — into `ShortcutKit` as `GlobalHotKeyMonitor.swift` (merged from QSP's `HotKeyService` + LaunchAway's design; consumes the existing `CustomKeyboardShortcut`). `ShortcutKit`'s `KeyboardEventMonitor` was `addLocalMonitorForEvents` (app-local only) — which is *why* every agent app hand-rolled the global part. Additive; `swift build` clean; all 21 ShortcutKit tests pass. Logged per-app borrow/contribute lists to each project's `docs/CROSS-POLLINATION.md` and a family plan to `zPackages/docs/plans/CROSS-POLLINATION-hotkey-panel-family.md`.
**Rationale:** App→app copying created the drift; the fix is to consolidate into `zPackages` and feed the genuinely-better bits back. QSP's multi-ID Carbon engine + samplers are its unique contributions; LaunchAway's `ResolvedTheme` live-repaint fix + ClipSmart's `Color(light:dark:)`/notarization/launch-at-login are what QSP should borrow.
**QuickStatsPanel's own backlog from this audit (not yet done — see PROJECT_STATE Next):** (1) launch-at-login via `SMAppService` (from ClipSmart) — obvious gap for an always-on HUD; (2) adopt `SilentUpdateKit` + ClipSmart's `package-dmg.sh` — QSP has no update path and this also closes the D-019 notarization gate; (3) adopt `ShortcutKit` (incl. the new `GlobalHotKeyMonitor`) to delete QSP's hand-rolled `HotKeyService`/`HotKeyRecorderView`; (4) port LaunchAway's `ResolvedTheme` env-injection **before** enabling follow-system light/dark (QSP currently forces `.dark` — `Theme.swift:39` — so the latent repaint bug is dormant, not fixed); (5) ClipSmart's enum-derived Settings search over `StatKind`.
**Status:** ✅ Shared engine landed in `zPackages/ShortcutKit` (build+tests green) + findings logged in all four projects. No QuickStatsPanel *source* changed by this audit — its backlog items are queued, not applied.

### 2026-07-03 - D-022: Launch-at-login, notarization proven, and two hotkey/anchor bug fixes
**Context:** First execution pass on the D-021 backlog. Launch-at-login was the highest value÷effort item for an always-on HUD. While testing on screen the user found two regressions, and we then de-risked the D-019-flagged notarization gate.
**Decisions:**
- **Launch-at-login via `SMAppService.mainApp`** (`Views/SettingsView.swift`, new "Startup" section atop the Panel pane). The app is non-sandboxed, so `SMAppService.mainApp.register()/unregister()` enrols the **main bundle itself** — no login-helper target. The **OS login-item registry is the single source of truth**, so there is deliberately **no `AppSettings` mirror**: the `@State` toggle reconciles against `SMAppService.mainApp.status` on `.onAppear` (can't silently lie), status-guards its calls (a redundant `register()` throws), and re-syncs to the real status in `catch`. Pattern borrowed from ClipSmart.
- **Cursor anchor no longer overridden by a saved drag.** Root cause of the user's "At cursor gets stuck": D-016 persists a drag as `customPosition` which **always beat the anchor** in `PanelWindowController.origin(...)`, and the strip is `isMovableByWindowBackground` — so an accidental click-nudge of the background pinned it permanently (choosing any anchor cleared it, hence the top-center→cursor workaround). Fix is two-sided: **read-gate** (`PanelWindowController` — cursor mode always recomputes from the live pointer, ignoring any saved position) + **write-gate** (`AppDelegate.onPanelMoved` — don't persist drags while in cursor mode). Drag-to-place is intentionally retained for the *fixed* anchors, where "stays put" is expected.
- **Single shared Carbon hotkey handler.** Root cause of the user's "⌘,/Esc do nothing": each `HotKeyService` instance installed its **own** app-level Carbon handler, but same-target handlers are called **newest-first and stop at the first that returns `noErr`** — every handler did — so only the *last-registered* hotkey (pin, id 4) ever fired; dismiss(2)/settings(3) were shadowed. The old code comment ("Carbon dispatches every press to *all* handlers") was simply false. Fix: **one process-wide shared handler + a static `[UInt32: () -> Void]` id→callback registry**; dispatch by id, no install-order dependency. Also un-broke toggle-to-dismiss-while-visible.
- **Signing-cert Syncthing leak closed at the root.** `Config/Debug.local.xcconfig` is gitignored but was Syncthing-carried, leaking one Mac's keychain cert to another (build failed at the signing gate). Added `**/*.local.xcconfig` to the Syncthing **`.stignore`** (folder root, outside the repo) — fixes it fleet-wide (QSP, AHAP, Penumbra, MousePlus); `.stignore` itself syncs so both Macs converge.
- **⭐ Notarization PROVEN — D-019 gate closed.** Release archive → `-exportArchive` (method=developer-id, re-signs with Developer ID + hardened runtime + secure timestamp) → zip → `xcrun notarytool submit --wait` → **Accepted** → `stapler staple` → `spctl -a` = **`accepted / source=Notarized Developer ID`** → the notarized Release build launches and runs. **The private-framework linkage `-lIOReport` (D-019) and IOHID temp symbols (D-018) do NOT trip notarization and need NO `com.apple.security.cs.disable-library-validation`** — they resolve from the Apple-signed dyld shared cache, so Library Validation is satisfied. The `dlopen` fallback D-019 contemplated is unnecessary. notarytool keychain profile `quickstatspanel-notary` set up on the M1 Max (ASC key in `99-AUTH/`, `AuthKey_*.p8`).
**Consequences:** Touched `SettingsView.swift`, `PanelWindowController.swift`, `AppDelegate.swift`, `HotKeyService.swift` — committed `f71dda8` (0 warnings, all user-verified on screen). No persistence/model changes. Build/notarize output under the ignored `build/` dir.
**Remaining before an actual release (all mechanical now):** DMG packaging script (the DMG wrapper needs its own sign+notarize+staple), version bump `0.1.0 → 1.0.0`, set `LSApplicationCategoryType` (archive warns it's missing), `/check ship` pass.
**Status:** ✅ Launch-at-login + both fixes shipped & user-verified; notarization chain proven end-to-end. Push `f71dda8` pending.

### 2026-07-12 - D-023: Public release under PolyForm Noncommercial 1.0.0
**Context:** v1.0.0 shipped but the repo was private, so the GitHub release download links weren't publicly reachable (needed for apps.lucesumbrarum.com). User chose to make the repo public, but wanted third-party use restricted to noncommercial.
**Decisions:**
- **License: PolyForm Noncommercial License 1.0.0** (`LICENSE`, verbatim from the canonical PolyForm repo tag `1.0.0`, with the user's copyright as the `Required Notice:` line). Chosen over CC BY-NC (content-oriented, no patent grant, CC advises against it for software) and a hand-rolled custom license (error-prone). Grants use/modify/distribute for any noncommercial purpose; commercial use reserved to the author. This is **source-available, not OSI "open source"** — GitHub badges it "Other," which is expected. README carries a license badge + section.
- **Repo made public** (`gh repo edit --visibility public`) after a full-history secret audit came back clean (no keys/certs/tokens/passwords; commits use GitHub's noreply email so the personal address isn't exposed).
- **No signing identifiers in tracked files.** Before going public, scrubbed the hardcoded ASC notary Key ID + Issuer UUID from `package-dmg.sh` + 4 docs; the script now derives the Key ID from the `AuthKey_*.p8` filename and reads the issuer from `99-AUTH/notary-issuer` (or `$NOTARY_ISSUER`). The `.p8` private key was never committed. `.gitignore` now blocks `*.p8/*.p12/*.cer/*.mobileprovision`. **Note:** the old identifiers remain in git history but are inert without the private key — key rotation is optional, not required (an earlier "must rotate" framing was an overreaction).
- **Pruned copied-in universal Directions docs from the repo** (cookbook, numbered guides, commands, mcp-templates, hooks — 178 files). They duplicated the already-public `Xpycode/LLM-Directions` master and contradicted the read-on-demand policy. Kept project-specific docs (PROJECT_STATE, decisions, sessions, specs, TASKS, ideas). Untracked personal `.mcp.json`. 335 → 157 tracked files.
**Consequences:** Repo public at https://github.com/Xpycode/QuickStatsPanel under PolyForm-NC. Commits `1ce15aa` (README screenshot gallery), `86e78b0` (license + notary-ID scrub), `31171d7` (prune docs + untrack .mcp.json).
**Status:** ✅ Public, licensed, audited clean, tidied. Open: point the website download at the public release URL.
